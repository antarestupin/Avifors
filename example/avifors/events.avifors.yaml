event:
    arguments:
        name: string
        attributes: [string]
    outputs:
        -
            template: example/implementation/event/event.template.php
            output: example/output/Event/{{ name | pascalcase }}.php

events: [event]

events_test:
    arguments:
        events:
            -
                id: number
                name: string
                attributes:
                    _default: [date]
                    _contents: [string]
    outputs: |
        {% for event in events %}
        -
            template: example/implementation/event/event.template.php
            arguments: {{ event | json }}
            output: example/output/Event/{{ event.name | pascalcase }}.php
        {% endfor %}
